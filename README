== Rwebta GemPlugin

You should document your project here.

1.Include cucumber and rspec for BDD testing
2.Use the Rake to implemente test case and suite
3.Refactor Pages boject module for every thing is resource

Install:
1.安装 ruby 1.9.2/1.8.7
2.gem update --system
3.解压 DevKit-tdm-32-4.5.1-20101214-1400-sfx 到某一个路径
   a.进入解压的路径
   b.运行ruby dk.rb init
   c.ruby dk.rb review(看一下输出的路径是否是你的ruby 安装路径)
   d.ruby dk.rb install
4.安装rewbat
    1.拷贝rwebat-1.8.9.gem 到本地目录
    2.进入本地目录运行下列命令
      gem install rwebat-1.8.9.gem
5.拷贝chromedriver_win_22_0_1203_0b下的chromedriver到本地某一目录
  拷贝IEDriverServer_Win32_2.25.1下的IEDriverServer到本地某一目录
  比如（放到bdriver文件夹下）
  添加bdriver到Path里（可能需要重启）

如果提示ffi安装错误：
   运行gem install ffi --version "1.0.9"

如果rwebat-1.8.9的文档没安装成功
 再次运行 gem install rwebta-1.8.4.gem
 
6.如果要使用远程：
   1.selenium-server-standalone-2.25.0 放置到远程server
   2.双击启动
 
Use :

 
 


